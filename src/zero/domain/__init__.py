"""Domain layer — pure types and rules.

This package depends on nothing else inside Zero (only stdlib + typing).
It is imported by ``app/``, ``persistence/``, and ``adapters/`` but
imports none of them.
"""
