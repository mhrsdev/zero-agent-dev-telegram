"""Application layer — use cases that orchestrate domain + persistence.

This package depends on :mod:`zero.domain` and on persistence
interfaces (Protocols declared in domain). It does not import from
:mod:`zero.adapters`.
"""
