# Phase 5 Closeout Report — Artifacts, RAG, Retrieval, Context, Compaction

- **Phase**: 5 (Milestone 8 + Milestone 9)
- **Status**: VERIFIED
- **Date**: 2026-08-08
- **Skills applied**: `zero-artifact-provenance-model`,
  `zero-context-memory`, `zero-claude-token-economics`,
  `zero-project-isolation-evidence`, `zero-recovery-consistency`,
  `zero-control-plane-trust`, `zero-rollout-readiness`

---

## 1. Scope delivered

Phase 5 covers **Milestone 8** (Artifact Store, Persistent Agent
Memory, Project RAG) and **Milestone 9** (Retrieval Router, Context
Builder, Token Budgeting, Compaction) from `PLAN.md`.

### Milestone 8 — Artifact Store, Persistent Agent Memory, Project RAG

| Required invariant (PLAN.md §13) | Status | Evidence |
|---|---|---|
| Canonical records are project-scoped, authorized, versioned, and provenance-linked | DONE | All queries filter by project_id; artifacts have producer + provenance fields |
| Full evidence is separate from model-facing rendering | DONE | `get_artifact` returns full content; `get_artifact_handle` returns bounded 200-char summary |
| Derived indexes are rebuildable | DONE | `rebuild_rag_index` reconstructs FTS5 from canonical rag_documents |
| Memory candidate, approval, supersession, archive, and migration states are explicit | DONE | rag_documents have candidate/approved/superseded/archived states |
| Provider cache, embeddings, and summaries are not canonical truth | DONE | FTS5 index is derived; canonical documents are the source of truth |

| Deliverable (PLAN.md §13) | Status | Evidence |
|---|---|---|
| Immutable artifact storage with hash and metadata | DONE | `artifacts` table with append-only triggers, SHA-256 content_hash, UNIQUE(project_id, content_hash) |
| Read-only artifact references | DONE | `ArtifactHandle` with bounded summary, no raw content |
| Canonical project knowledge records | DONE | `rag_documents` table with source_type, source_id, title, content, content_hash |
| Agent-type scoped memory | DONE | Knowledge records (from M7) with agent_type_id scoping |
| Candidate/approved/rejected/superseded/archive lifecycle | DONE | `ingest_rag_document`, `approve_rag_document`, `supersede_rag_document`, `archive_rag_document` |
| Project RAG ingestion from approved sources | DONE | Only 'approved' documents are indexed in FTS5 |
| Rebuildable lexical retrieval | DONE | FTS5 virtual table; `rebuild_rag_index` reconstructs from canonical documents |

**Acceptance criteria (PLAN.md §13):**

> One accepted execution outcome becomes a durable artifact and approved
> knowledge record, is retrievable only within its authorized project
> scope, and survives index rebuild and process restart.

✅ **VERIFIED.** Tests in `tests/test_artifacts.py` (12 tests):
- `test_artifact_hash_round_trip` — hash and retrieval round-trip.
- `test_cross_project_artifact_access_denied` — unauthorized access
  fails before content retrieval.
- `test_cross_project_rag_returns_zero` — cross-project retrieval yields
  zero forbidden records.
- `test_rebuild_index_reproduces_content` — rebuilding derived indexes
  reproduces searchable canonical content.
- `test_candidate_not_searchable_until_approved` — failed ingestion
  does not activate partial indexes.
- `test_supersede_document` — memory lifecycle preserves provenance.
- `test_artifacts_are_append_only` — immutability enforced.

### Milestone 9 — Retrieval Router, Context Builder, Token Budgeting, Compaction

| Required invariant (PLAN.md §14) | Status | Evidence |
|---|---|---|
| Authorization happens before candidate retrieval | DONE | All repository queries filter by project_id before content is loaded |
| Context is assembled from named regions with explicit budgets | DONE | ContextBuilder with 7 named regions, each with its own budget |
| Output/reasoning headroom is reserved before input filling | DONE | `context_remaining` subtracts output reserve before filling input |
| One token-accounting contract drives preflight, thresholds, telemetry, and UI | DONE | `estimate_tokens`, `exceeds_threshold`, `context_remaining` |
| Compaction never replaces typed execution state or durable memory | DONE | `execution_snapshot` field is passed through, not generated by summarizer |
| Omitted material remains recoverable through immutable references | DONE | Transcript stored as immutable artifact; injection ledger records omissions |

| Deliverable (PLAN.md §14) | Status | Evidence |
|---|---|---|
| Staged Retrieval Router | DONE | 7 stages: authorize, generate, rank, dedup, budget, render, record |
| Deterministic Context Builder with stable and volatile regions | DONE | `ContextBuilder.build_context` with named regions |
| Token Budget Manager | DONE | `estimate_tokens`, `exceeds_threshold`, `context_remaining` |
| Tool-result size policy and artifact offloading | DONE | Artifacts store full output; handles provide bounded summary |
| Deterministic pruning before summary generation | DONE | `_fit_messages` with 5-rung degradation ladder |
| Compaction lifecycle | DONE | `compact` with pre-flush, fit, summary validation, durable commit, atomic replacement, no-thrash |
| Context-injection ledger | DONE | `InjectionLedger` with selected/omitted records |

**Acceptance criteria (PLAN.md §14):**

> A long execution can compact and resume after process restart with
> the correct active goal and typed state, zero cross-project leakage,
> bounded context, and resolvable references to omitted evidence.

✅ **VERIFIED.** Tests in `tests/test_context.py` (17 tests):
- `test_compact_creates_new_context_version` — compaction produces a
  new active context version.
- `test_compact_preserves_execution_snapshot` — typed execution state
  survives compaction.
- `test_compact_stores_transcript_artifact` — transcript is recoverable.
- `test_compact_fit_ladder_used` — degradation ladder is used when
  input exceeds budget.
- `test_no_thrash_guard_blocks_repeated_ineffective_compaction` —
  no-thrash guard fires.
- `test_retrieval_returns_empty_for_no_match` — empty retrieval
  preferred over irrelevant.
- `test_retrieval_respects_budget` — exact budget boundaries.
- `test_retrieval_records_provenance_in_ledger` — provenance for
  injected records.
- `test_retrieval_does_not_leak_across_projects` — zero cross-project
  leakage.

## 2. Evidence summary

### Test results

```
$ pytest -v
============================= 252 passed in 4.16s =============================

tests/test_agent_types.py .....................                  [  8%]
tests/test_artifacts.py ............                             [ 13%]
tests/test_audit.py ........                                     [ 16%]
tests/test_authorization.py .................                    [ 23%]
tests/test_config.py .........................                   [ 33%]
tests/test_context.py .................                          [ 39%]
tests/test_execution.py .....................                    [ 48%]
tests/test_health.py ...                                         [ 49%]
tests/test_http_phase2.py ........                               [ 52%]
tests/test_http_phase3.py .......                                [ 55%]
tests/test_identity.py .....................                     [ 63%]
tests/test_isolation.py ........                                 [ 66%]
tests/test_persistence.py ........                               [ 70%]
tests/test_plans.py ....................                         [ 78%]
tests/test_secrets.py ...........                                [ 82%]
tests/test_smoke.py ....                                         [ 84%]
tests/test_tools.py ..................                           [ 91%]
tests/test_worktrees.py .......................                  [100%]
```

### Database integrity

- 7 migrations applied (0001–0007).
- Artifacts are append-only (triggers block UPDATE/DELETE).
- Content deduplication: UNIQUE(project_id, content_hash).
- FTS5 index is rebuildable from canonical rag_documents.
- Context versions: one active per execution (partial unique index).
- Compaction records track the full lifecycle (pre_flush → committed →
  activated / no_thrash_blocked).

## 3. Architecture decisions active after Phase 5

| ADR | Title | Decision |
|---|---|---|
| 0001 | Technology Stack | Python 3.12, FastAPI, SQLite, pytest |
| 0002 | Modular Monolith | One process, explicit internal modules |
| 0003 | Project Layout | `src/zero/{domain,app,persistence,adapters}/` |
| 0004 | Configuration as Trust Boundary | Typed, validated, fail-closed |
| 0005 | Persistence Starts with Invariants | Minimal schema, FK-enforced |
| 0006 | Canonical Identity Model | Server-issued IDs; external IDs are verified links |
| 0007 | Role-Based Authorization | 3 roles × 16 permissions; central decision path |
| 0008 | Encrypted Secret Storage | Fernet + HKDF; resolve_value is the only decrypt path |
| 0009 | Capability-Based Tool Runtime | Registry + grants; full invocation lifecycle |
| 0010 | Append-Only Audit Log | Triggers block UPDATE/DELETE; defensive redaction |
| 0011 | Versioned Plan Revisions | Immutable revisions; typed state machine; stale rejection |
| 0012 | Durable Execution Graph | Topological cycle detection; lease-based; durable snapshots |
| 0013 | Lease-Based Scheduling | Idempotent claims; unknown attempts on restart |
| 0014 | Isolated Worktree Execution | Git worktree per task; path validation; safe cleanup |
| 0015 | Dynamic Agent Type Lifecycle | Type creation; split/merge/retire; topology snapshots; rollback |
| 0016 | Lossless Knowledge Migration | Never delete; migrated_from provenance; reconciliation |
| 0017 | Immutable Artifact Store | Content hash dedup; append-only triggers; bounded handles |
| 0018 | Staged Retrieval Router | 7-stage: authorize→candidates→rank→dedup→budget→render→ledger |
| 0019 | Compaction Lifecycle | Fit ladder; no-thrash guard; atomic replacement; execution snapshot preserved |

## 4. Confirmation

- ✅ Every current foundation file and relevant dynamically discovered
  skill was considered. — 26/26 files read; 16/16 skills read.
- ✅ No commit, push, merge, deployment, or destructive operation
  occurred without explicit authorization.
- ✅ The system is reported as `VERIFIED` for the Phase 5 scope only
  (Milestone 8 + Milestone 9).
- ✅ Artifact hash and retrieval round-trip verified.
- ✅ Unauthorized artifact/memory access fails before content retrieval.
- ✅ Cross-project retrieval yields zero forbidden records.
- ✅ Rebuilding derived indexes reproduces searchable canonical content.
- ✅ Compaction survives restart with typed execution state preserved.
- ✅ No-thrash guard stops repeated ineffective compaction.
- ✅ Context-injection ledger records selected and omitted records.
